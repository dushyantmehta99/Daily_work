<?xml version="1.0" encoding="UTF-8"?>
<screen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/moqui-conf-3.xsd">

    <actions>
        <entity-find entity-name="tutorial.Tutorial" list="tutorialList">
            <search-form-inputs/>
        </entity-find>
    </actions>

    <widgets>

        <form-list name="ListTutorials" list="tutorialList" header-dialog="true" skip-form="true">

            <auto-fields-entity entity-name="tutorial.Tutorial" field-type="find-display"/>

            <field name="description">
                <header-field show-order-by="true">
                    <text-find hide-options="true"/>
                </header-field>
                <default-field>
                    <display/>
                </default-field>
            </field>

            <!-- EDIT BUTTON -->
            <field name="edit">
                <default-field>
                    <link text="Edit" icon="fa-pencil" style-inline="true" url="EditTutorial">
                        <parameter name="tutorialId" from="tutorialId"/>
                    </link>
                </default-field>
            </field>

            <!-- DELETE BUTTON WITH CONFIRMATION -->
            <field name="delete">
                <default-field>
                    <link text="Delete" icon="fa-trash" style-inline="true"
                          url="deleteTutorial"
                          confirmation="Are you sure you want to permanently delete this tutorial?">
                        <parameter name="tutorialId" from="tutorialId"/>
                    </link>
                </default-field>
            </field>

        </form-list>

        <container-dialog id="CreateTutorialDialog" button-text="Create Tutorial">
            <form-single name="CreateTutorial" transition="createTutorial">
                <auto-fields-entity entity-name="tutorial.Tutorial" field-type="edit"/>
                <field name="submitButton">
                    <default-field title="Create">
                        <submit/>
                    </default-field>
                </field>
            </form-single>
        </container-dialog>

    </widgets>

    <!-- EDIT TRANSITION -->
    <transition name="EditTutorial" read-only="true">
        <default-response url="../EditTutorial"/>
    </transition>

    <!-- CREATE TRANSITION -->
    <transition name="createTutorial">
        <service-call name="tutorial.TutorialServices.create#Tutorial"/>
        <default-response url="." message="Tutorial created successfully"/>
        <error-response url="." message="${message}"/>
    </transition>

    <!-- DELETE TRANSITION WITH ERROR HANDLING -->
    <transition name="deleteTutorial">
        <service-call name="tutorial.TutorialServices.delete#Tutorial"/>
        <default-response url="." message="${message}"/>
        <error-response url="." message="${message}"/>
    </transition>

</screen>