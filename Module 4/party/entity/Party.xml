<?xml version="1.0" encoding="UTF-8"?>
<entities xmlns="http://moqui.org/xsd/entity-definition-3.0.xsd">

    <!-- Party: represents any person or organization -->
    <entity entity-name="Party" package-name="party">
        <field name="partyId"         type="id"        is-pk="true"/>
        <field name="partyTypeEnumId" type="id"/>
        <field name="createdDate"     type="date-time"/>
    </entity>

    <!-- Person: additional details for individual persons -->
    <entity entity-name="Person" package-name="party">
        <field name="partyId"     type="id"         is-pk="true"/>
        <field name="firstName"   type="text-short"/>
        <field name="lastName"    type="text-short"/>
        <field name="dateOfBirth" type="date"/>
        <relationship type="one" related="party.Party" fk-name="PERSON_PARTY">
            <key-map field-name="partyId"/>
        </relationship>
    </entity>

    <!-- PartyGroup: details for companies and organizations -->
    <entity entity-name="PartyGroup" package-name="party">
        <field name="partyId"     type="id"          is-pk="true"/>
        <field name="groupName"   type="text-short"/>
        <field name="description" type="text-medium"/>
        <relationship type="one" related="party.Party" fk-name="PARTY_GRP_PARTY">
            <key-map field-name="partyId"/>
        </relationship>
    </entity>

    <!-- ContactMech: stores email, phone, or address info -->
    <entity entity-name="ContactMech" package-name="party">
        <field name="contactMechId"     type="id"          is-pk="true"/>
        <field name="contactMechTypeId" type="id"/>
        <field name="infoString"        type="text-medium"/>
        <field name="createdDate"       type="date-time"/>
    </entity>

    <!-- PartyContactMech: links a Party to a ContactMech -->
    <entity entity-name="PartyContactMech" package-name="party">
        <field name="partyId"       type="id"        is-pk="true"/>
        <field name="contactMechId" type="id"        is-pk="true"/>
        <field name="fromDate"      type="date-time" is-pk="true"/>
        <field name="thruDate"      type="date-time"/>
        <relationship type="one" related="party.Party" fk-name="PCM_PARTY">
            <key-map field-name="partyId"/>
        </relationship>
        <relationship type="one" related="party.ContactMech" fk-name="PCM_CONTACT_MECH">
            <key-map field-name="contactMechId"/>
        </relationship>
    </entity>

</entities>
